# React CV Website

A modern one-page CV website built with React and Vite.

## Run locally

```bash
npm install
npm run dev
```

## Build for production

```bash
npm run build
```

## Customize

Edit the `profile` object at the top of `src/App.jsx` to update your name,
contact details, experience, projects, skills, and education.
